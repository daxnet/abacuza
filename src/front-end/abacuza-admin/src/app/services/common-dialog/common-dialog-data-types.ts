
/**
 * Represents the type of the common dialog.
 *
 * @export
 * @enum {number}
 */
export enum CommonDialogType {
    Information,
    Confirm,
    Warning,
    Error
}

export enum CommonDialogResult {
    OK,
    Cancel,
    Yes,
    No
}